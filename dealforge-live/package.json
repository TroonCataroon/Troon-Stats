{
  "name": "dealforge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build && node scripts/smoke.mjs"
  }
}
